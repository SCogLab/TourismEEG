<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.BCIVoyager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme" >

        <activity android:name=".Activity2Connexion"
            android:label="Connexion"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/AppTheme"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".Activity3Calibration"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".Activity4LaunchXP"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".Activity5Images"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".Activity6Results"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".ActivitySettings"
            android:label="Settings"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/AppTheme"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name=".Activity1Launchscreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>